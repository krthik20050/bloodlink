import { NextResponse } from "next/server"; import { store } from "@/lib/store"; import { matchDonors } from "@/lib/matching"; import { queueNotifications } from "@/lib/notifications";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const request=store.requests.find(r=>r.id===id);if(!request)return NextResponse.json({error:"Request not found"},{status:404});if(request.status!=="OPEN")return NextResponse.json({error:"Request is not open"},{status:409});const result=matchDonors(request,store.donors,store.notifications);const notifications=await queueNotifications(request.id,result.selected.map(x=>x.donorId),1);console.info(JSON.stringify({event:"matching_completed",requestId:id,selected:result.selected.length}));return NextResponse.json({result,notifications:notifications.map(n=>({id:n.id,donorId:n.donorId,waveNumber:n.waveNumber,response:n.response,actionToken:n.actionToken}))});}
