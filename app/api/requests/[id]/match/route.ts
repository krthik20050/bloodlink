import { NextResponse } from "next/server"; import { listDonors, listNotifications, getRequest } from "@/lib/supabase/repository"; import { matchDonors } from "@/lib/matching"; import { queueNotifications } from "@/lib/notifications"; import { getSupabaseUser } from "@/lib/supabase/server";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const user=await getSupabaseUser();if(!user)return NextResponse.json({error:"Authentication required"},{status:401});const {id}=await params;const request=await getRequest(id);if(!request)return NextResponse.json({error:"Request not found"},{status:404});if(request.requesterId!==user.id)return NextResponse.json({error:"Request not found"},{status:404});if(request.status!=="OPEN")return NextResponse.json({error:"Request is not open"},{status:409});const result=matchDonors(request,await listDonors(),await listNotifications());const notifications=await queueNotifications(request.id,result.selected.map(x=>x.donorId),1);console.info(JSON.stringify({event:"matching_completed",requestId:id,selected:result.selected.length}));return NextResponse.json({result,notifications:notifications.map(n=>({id:n.id,donorId:n.donorId,waveNumber:n.waveNumber,response:n.response}))});}
