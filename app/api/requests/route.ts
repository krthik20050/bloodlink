import { NextResponse } from "next/server"; import { z } from "zod"; import { bloodGroups } from "@/lib/domain"; import { id,store } from "@/lib/store";
const schema=z.object({bloodGroup:z.enum(bloodGroups),unitsRequired:z.number().int().min(1).max(10).default(1),hospital:z.string().min(2).max(100),latitude:z.number().gte(-90).lte(90),longitude:z.number().gte(-180).lte(180),urgency:z.enum(["ROUTINE","URGENT","EMERGENCY"])});
export async function GET(){return NextResponse.json(store.requests);} export async function POST(req:Request){const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid request",details:p.error.flatten()},{status:400});const x=p.data;const request={id:id("request"),requesterId:"demo-requester",bloodGroup:x.bloodGroup,unitsRequired:x.unitsRequired,hospital:x.hospital,location:{latitude:x.latitude,longitude:x.longitude},urgency:x.urgency,status:"OPEN" as const,createdAt:new Date().toISOString()};store.requests.push(request);console.info(JSON.stringify({event:"request_created",requestId:request.id}));return NextResponse.json({request,bloodBanks:[{name:"Demo Community Blood Centre",distanceKm:1.8,bloodGroup:x.bloodGroup,availability:"Reported available — confirm with the blood bank.",source:"Mock provider",lastUpdated:new Date().toISOString()}]},{status:201});}
