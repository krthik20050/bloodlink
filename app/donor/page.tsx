"use client"; import { useState } from "react"; import { bloodGroups } from "@/lib/domain";
export default function DonorPage(){const [message,setMessage]=useState("");async function submit(formData:FormData){const payload={name:String(formData.get("name")),bloodGroup:String(formData.get("bloodGroup")),latitude:Number(formData.get("latitude")),longitude:Number(formData.get("longitude")),lastDonationDate:String(formData.get("lastDonationDate"))||null,notificationConsent:formData.get("consent")==="on",contact:String(formData.get("contact"))};const res=await fetch("/api/donors",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});setMessage(res.ok?"Donor profile saved. You will only receive relevant requests.":"Could not save the profile. Check each field and try again.")}return <main className="shell"><section className="panel"><p className="kicker">Donor profile</p><h1>Be ready when it matters.</h1><p>We use the details below only to filter relevant notifications. This is not medical clearance.</p><form action={submit}><label className="field">Name<input name="name" required minLength={2}/></label><label className="field">Email for confirmed matches<input name="contact" type="email" required/></label><label className="field">Blood group<select name="bloodGroup">{bloodGroups.map(x=><option key={x}>{x}</option>)}</select></label><label className="field">Last donation date (leave blank if none)<input name="lastDonationDate" type="date"/></label><label className="field">Latitude<input name="latitude" type="number" step="any" defaultValue="11.2588" required/></label><label className="field">Longitude<input name="longitude" type="number" step="any" defaultValue="75.7804" required/></label><label className="field"><span><input name="consent" type="checkbox" required/> I agree to receive relevant blood-donation notifications.</span></label><button>Save donor profile</button></form>{message&&<p className="notice">{message}</p>}</section></main>}
